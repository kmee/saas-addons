http:
  routers:
    {% for build in builds %}
    {{build[0]}}:
      entryPoints:
        - websecure
      rule: 'Host(`{{build[0]}}.{{remote_domain}}`)'
      service: {{build[1]}}
    {% endfor %}

  ## SERVICES ##
  services:
  {% for operator in operators %}
    {{operator[0]}}:
      loadBalancer:
        servers:
          - url: {{operator[1]}}
  {% endfor %}
