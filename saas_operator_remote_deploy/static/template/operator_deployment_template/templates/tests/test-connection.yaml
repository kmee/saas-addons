apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "erplivre14-odoo-hml.fullname" . }}-test-connection"
  labels:
    {{- include "erplivre14-odoo-hml.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "erplivre14-odoo-hml.fullname" . }}:8069']
  restartPolicy: Never
